let drone = stampit({
        type: 'drone',
        age: 0,
        generation: 1,
    }, {
        //state
        energy: config.droneEnergy
        // spawnTimer: config.spawnRate
    }

    //enclosed
);
